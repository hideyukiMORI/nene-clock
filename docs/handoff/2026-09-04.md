# 引き継ぎ書 2026-09-04 — 見た目が変わった。次は M1（中身）

> 次にこのリポを触る人（人間でも AI でも）へ。**このファイルだけで現在地を掴めるように書く。**
> 前版は [2026-09-03](2026-09-03.md)（初日・M0.1 完了時点）。本版が最新。

---

## 0. 30 秒で現在地

| | |
| --- | --- |
| 製品 | Java 21 / Swing のデスクトップ時計。`hideyukiMORI/nene-clock`（public） |
| 状態 | ✅ **M0.2 完了**（同梱書体 30・背景色・**枠なし窓**・**設定モーダル**）。`main` 緑・未マージ PR なし |
| 見た目 | 枠の無い窓に時刻と日付だけ。ホバーで右上に 3 アイコン。ギア → 設定モーダル |
| 動かないもの | ストップウォッチ（FR-010）・タイマー（FR-020）。**意図的に未実装。置き場所も未定** |
| 実機確認 | ✅ WSLg で目視済み（記録は [quality/gate-proofs.md](../quality/gate-proofs.md) 第 11 節） |
| 完了定義 | `./gradlew check` **ただ 1 本**。CI は同じタスクを呼ぶだけ |
| 次の 1 手 | #15 ストップウォッチ。**先に「どこに置くか」を決める**（タブが無くなったため） |

```bash
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew check   # 唯一の入口
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 ./gradlew run     # 起動
./gradlew spotlessApply                                        # 整形を直す
```

デザインの正本（10 アートボード）: https://claude.ai/code/artifact/df5a6479-a4bd-4bbe-a656-80b7781abd3d

---

## 1. 初日から変わったこと（ここだけ読めば差分が分かる）

| | 2026-09-03 | 2026-09-04 |
| --- | --- | --- |
| 書体 | 実行環境から読む（`GraphicsEnvironment`） | **同梱 30 書体の列挙**（ADR 0006） |
| 色 | 文字色のみ | 文字色＋**背景色**（ADR 0007） |
| 窓 | タイトルバー＋タブ | **枠なし・ホバークローム**（ADR 0008） |
| 設定 | Settings タブ | **ギア → モーダル**（一覧 / 書体 / 色） |
| 例外区画 | 3 つ | **2 つ**（`platform.txt` を外すモジュールが 0） |
| 保存形式 | v2 | **v4**（v1 / v2 / v3 から移行できる） |

消えた型: `FontFamily` 系 3・`FontCatalogPort`・`SettingsView`・`MainFrame`・`SettingsPanel`。

---

## 2. 🔴 いちばん伝えたいこと — 「`check` が緑」は「動く」ではない

2026-09-04 に、**単体テストが全部緑のまま壊れていた箇所を 4 件**見つけた。
どれも**窓を出して画面を見るまで分からなかった**（詳細は gate-proofs 第 11 節）。

| 何が | なぜテストで見えなかったか |
| --- | --- |
| 同梱書体 4 つが Thin / ExtraLight で描かれる | 検査は「読み込めるか」しか見ていなかった |
| 100pt で時刻が「05:14:..」と切れる | 窓の幅と文字の幅の関係は描かないと分からない |
| ポインタの上に窓が出るとクロームが永久に出ない | イベントの順序は実機の窓マネージャが決める |
| 色見本が右へ寄る／分類チップが潰れる | レイアウトの結果は描画してからでないと分からない |

⚠️ **UI を触ったら、必ず `./gradlew run` して目視すること。** そして見たことを記録に残す。
`check` の緑は、見た目について何も言っていない。

---

## 3. ゲートの地図（担当を重ねない）

初日から変わったのは 2 行だけ。

| 層 | 道具 | 見ているもの |
| --- | --- | --- |
| コンパイル | `javac -Xlint:all -Werror --release 21` | 型・`sealed` の網羅性・警告ゼロ |
| null | Error Prone + NullAway（error） | `null` の意味の固定 |
| **メソッド単位** | forbidden-apis | `now()` / 乱数 / `GraphicsEnvironment` / `System.exit` … |
| **パッケージ単位** | ArchUnit | レイヤ境界・Swing/prefs の隔離・可変フィールド |
| 構造 | Checkstyle | 複雑度・1 ファイル 1 主要宣言・広い catch・**MagicNumber** |
| 整形 | Spotless + palantir-java-format | 唯一の正本 |
| 固有規約 | `validateConformance`（CNF-001..011） | 総称名・waiver・`default`・render 経路・文書整合 |
| 検査自身 | `:build-logic:test` | CNF 各規則の正例・反例 |
| カバレッジ | JaCoCo | core 2 モジュールで分岐 90% 下限 |
| **同梱書体**（新） | `:adapters:font-catalog:test` | **ファイルの存在・SHA-256・描ける形か・既定が Regular か** |

---

## 4. 🔑 例外区画は 2 つになった（初日は 3 つ）

| 区画 | 外している署名 | 許されること | なぜ |
| --- | --- | --- | --- |
| `:adapters:system-time` | `determinism.txt` | 現在時刻・既定ゾーンを読む | ARC-007 の唯一の窓口 |
| `:app` | `process-control.txt` ＋ bundled `jdk-system-out` | 端末出力・`System.exit` | 窓が出る前の唯一の伝達経路（ADR 0005） |

**`:adapters:font-catalog` の例外は畳んだ**（書体を同梱したので `GraphicsEnvironment` が要らなくなった）。
🔴 **3 つ目を作らない。** 必要になったら ADR で問い直す。

可変状態の隔離区画は 2 つのまま（`:ui:swing` の Swing 部品の内部 ／ `:core:application` の `SettingsHandler`）。

---

## 5. 地雷（この 2 日で実際に踏んだ）

初日の 8 件（`.gitignore` の `build/`・included build・precompiled script plugin・`record` の正準コンストラクタ・
`switch` の `default`・`MissingSwitchDefault`・NullAway・テスト引数 4 個）は
[2026-09-03 版](2026-09-03.md)を見ること。以下は 2026-09-04 に足されたもの。

1. **Java 21 は可変フォントの軸を選べない。** `Font[wght].ttf` は既定のアウトラインで描かれ、
   Google の可変フォントはその既定が最も細いマスタのことがある。
   **書体を足すときは `everyTypefaceRendersAtRegularWeight` を必ず通すこと**
2. **`BoxLayout` は子の `alignmentX` が混ざると横位置が崩れる。** 全部の子に同じ寄せを言う
3. **`BoxLayout` は子を縦に潰すことがある。** 札を横に並べるだけなら `FlowLayout` のほうが素直
4. **`Container.getMaximumSize()` は既定で推奨サイズを返す。** 広げたい子には `setMaximumSize` を言う
5. **`mouseEntered` は当てにならない。** ポインタが既にある位置に窓が出ると飛ばない。`mouseMoved` も見る
6. **Checkstyle の `MagicNumber` は描画コードによく当たる。** 色も座標も定数にする
   （`static final` の初期化子の中なら通る）
7. **匿名クラスのメソッドに `/** */` を書くと Error Prone の `NotJavadoc` が落とす。** 行コメントにする
8. **Swing 部品は継承せず内包する。** 継承すると `serial` 警告が出て `-Werror` で落ちる

---

## 6. 環境メモ

| | |
| --- | --- |
| JDK | `/usr/lib/jvm/java-21-openjdk-amd64`。システム既定は Java 11 なので `JAVA_HOME` を指定する |
| Gradle | Wrapper 同梱（9.7.1・配布物の SHA-256 を固定） |
| 表示 | ✅ WSLg で動く（`DISPLAY=:0`）。角丸（`setShape`）も効いた |
| 目視の取り方 | `import -window <id>` で窓を撮れる（**root は撮れない**。WSLg は rootless）。<br>窓 id は `xwininfo -root -tree \| grep "NeNe Clock"` |
| 操作の自動化 | `java.awt.Robot` で動かせる。**最初のクリックは窓のフォーカスに食われる**ので 2 回押す |
| 表示が無いとき | `./gradlew run` は 2 行のメッセージを出して**終了コード 1**（#30 で修正済み） |

---

## 7. 次の 1 手

1. **#15 ストップウォッチ。** 実装より先に「**どこに置くか**」を決める。
   タブは無くなった（FR-050 に「実装の直前に決め直す」と書いてある）。
   クロームにもう 1 つ経路を足すのか、モーダルに画面を足すのか、別の窓にするのか
2. #16 タイマー（FR-020）。**目標時刻からの計算**で求める（SWG-005）
3. #18 SWG-004 / SWG-005 の機械強制を `planned` → `active`
4. #19 SHA-256 dependency verification（QLT-011 の planned 部分）

施主に確認したいこと（急がない）:

- 既定の配色は明るいまま（背景 `#F5F2EB` / 文字 `#000000`）でよいか。
  暗い既定（`#1A1917` / `#EFEAE1`）にもできる。デザインには両方描いてある
- 窓の手動リサイズが無いこと（大きさは設定に従う）でよいか

---

## 8. 作業の型（2 日で 15 PR 回して機能した）

1. **Issue を先に立てる。**「問題と根拠 / 意図する結果 / 影響する規則 / 受け入れ条件 / 検証の計画 / やらないこと」
2. **見た目を変えるなら先にデザイン。** `/design` でキャンバスを作り、方向と捨てた案を残す
3. 設計そのものを変えるなら**先に** ADR と規範文書。実装と同じ変更で入れる
4. ブランチ → 最小の実装 → テスト → `./gradlew check`
5. **UI を触ったら `./gradlew run` で目視し、見たことを記録する**
6. **コミットごとに `git worktree add` で切り出して `check` を回す**
7. PR 本文は定型。**却下した選択肢と残るリスクを必ず書く**
8. CI 緑を確認してから squash merge → `main` を同期 → ブランチを消す

⚠️ ゲートが落ちたら**コードを直す**。閾値・除外・重大度を触るのは ADR 相当の判断（QLT-010）。
⚠️ `planned` を `active` と書き換えない。実装してから書き換える。
⚠️ **落ちたこと（規則 ID）だけを見ない。メッセージまで読む。** 2 日で 2 回、規約が自分について嘘をついていた。
