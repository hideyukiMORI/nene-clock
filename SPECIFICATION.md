# 製品仕様 — NeNe Clock

> Status: normative（規範）/ 2026-09-03 初版
> 本書は**何を作るか**の正本。**どう作るか**は [docs/](docs/) 配下の規範文書が正本であり、
> 両者が矛盾したら次の順で優先する。
>
> 1. 本書の機能要件（FR-NNN）
> 2. [docs/ARCHITECTURE_CONSTITUTION.md](docs/ARCHITECTURE_CONSTITUTION.md) の憲章規則
> 3. [docs/CODING_RULES.md](docs/CODING_RULES.md) の実装規則
> 4. 最も単純で保守しやすい実装

---

## 1. 概要

NeNe Clock は Java 21 / Swing で作る小さなデスクトップ時計である。

- 読んで分かる規模に保つ
- 公開リポジトリの実務サンプルとして成立させる
- WSLg を含む Linux 環境で動く
- ストップウォッチ・カウントダウンタイマーへ拡張できる構造にする

外部の実行時依存を持たない（JDK のみ）。

### 非目標

ネットワーク・クラウド同期・アカウント・データベース・トレイ常駐・OS 通知・
複数アラーム・英語以外の UI・プラグイン機構は初版では作らない。

---

## 2. 対象環境

| 区分 | 環境 |
| --- | --- |
| 主 | Windows 11 ＋ WSL2（Ubuntu）＋ WSLg |
| 副 | ネイティブ Linux デスクトップ、Windows の JVM |
| 未検証 | macOS（動くと**書かない**。QLT-012） |

実行時要件: Java 21 以上。ビルド: Gradle Wrapper 同梱。

---

## 3. 機能要件

### FR-001 — 現在時刻を表示する

主画面はローカルの現在時刻を表示する。表示は少なくとも 1 秒に 1 回更新される。

### FR-002 — 表示更新はちらつかない

更新のたびに画面全体が再構築される、点滅する、といった見え方をしない。

### FR-003 — 12 時間制と 24 時間制を切り替えられる

既定は 24 時間制。

```text
24 時間制: 13:45:09
12 時間制: 01:45:09 PM
```

### FR-004 — 秒の表示を切り替えられる

秒を隠したときの表記は `13:45` / `01:45 PM`。

### FR-005 — 日付の表示を切り替えられる

日付の表記は ISO の `yyyy-MM-dd`（例 `2026-09-03`）。表示しない状態は、
空文字ではなく型（`DateLine.Hidden`）で表す。

### FR-006 — 時刻の整形はロケールに依存しない

表示は既定ロケールに影響されない。整形には常に明示的なロケールを渡す。

---

### FR-010 — ストップウォッチ（M1・未実装）

開始・一時停止・再開・リセットを持つ。表示は `HH:MM:SS.mmm`。
計測には単調増加時刻（`System.nanoTime()` 相当）を使い、壁時計を使わない。
実行中のリセットは計測を止めてゼロへ戻す。

🔴 **M0 では実装しない。** 実装済みと書かないために、ここに未実装と書く。

### FR-020 — カウントダウンタイマー（M1・未実装）

時・分・秒の入力、開始・一時停止・再開・リセットを持つ。

- 負の値を受け付けない。ゼロ時間で開始しない
- 残り時間を負で表示しない
- 残り時間は**目標時刻からの計算**で求める。毎刻み 1 ずつ減らす形を正本にしない（SWG-005）
- ゼロに達したことを画面で明示する

🔴 **M0 では実装しない。**

---

### FR-030 — ウィンドウの挙動

- タイトルは `NeNe Clock`
- **枠を持たない**（`setUndecorated`）。見た目と操作は FR-047
- 初期 480 x 240、最小 320 x 160
- 常に最前面表示を切り替えられる
- 閉じたときに常駐スレッドを残さない

表示できない環境（X11 / Wayland が無い）で起動したときは、**原因を 1 行で伝えて
終了コード 0 以外で終わる**。スタックトレースを見せない。窓が出ていないのに
成功として返ることを許さない（QLT-012）。

### FR-040 — 設定項目と既定値

利用者が設定できるのは次の 9 つ。すべて設定画面（FR-045）から変更できる。

| 設定 | 型 | 既定値 |
| --- | --- | --- |
| 時刻表記 | `ClockFormat` | `HOUR_24` |
| 秒の表示 | `SecondsVisibility` | `SHOWN` |
| 日付の表示 | `DateVisibility` | `SHOWN` |
| 常に最前面 | `WindowTopmost` | `DISABLED` |
| 書体 | `Typeface` | `JetBrains Mono` |
| フォントサイズ | `FontSize` | 64 |
| 文字色 | `RgbColor` | `#000000` |
| 背景色 | `RgbColor` | `#F5F2EB` |
| 言語 | `Language` | `JAPANESE` |

選択肢が閉じているものは、真偽値の組み合わせではなくそれぞれ列挙として表す（JAV-002）。
値に不変条件があるものは専用の値型にし、生の `int` や `String` で持ち回らない（JAV-001）。

### FR-041 — フォントサイズの範囲

24 以上 160 以下。範囲外は例外ではなく型のある拒否として返す
（`FontSizeRejection.BELOW_MINIMUM` / `ABOVE_MAXIMUM`）。

### FR-043 — 書体

**書体はアプリが同梱する。実行環境が持っている書体は使わない**（ADR 0006）。

- 同梱するのは Google Fonts の **30 書体**（すべて SIL Open Font License）。
  ファイルは無改変で同梱し、出所（上流の commit と SHA-256）とライセンス本文を一緒に持つ
- 選択肢が閉じたので、書体は列挙 `Typeface` で表す（JAV-002）。
  保存されるのは列挙の定数名であり、環境の書体名ではない
- 既定は `JetBrains Mono`。等幅なので秒が変わっても桁の位置が動かない
- 書体は**雰囲気**（`TypefaceMood`: SANS / SERIF / MONO / DISPLAY / RETRO / HAND）で分類する。
  設定画面はこの分類で絞り込める

🔑 **この変更で「利用可能かどうか」が環境依存の事実でなくなった。**
どの実行環境でも同じ 30 書体が同じ見た目で描かれる。
実行環境の書体一覧を読む経路（`GraphicsEnvironment`）はアプリから消えた。

### FR-044 — 文字色

RGB の各成分が 0 以上 255 以下。範囲外は型のある拒否として返す
（`RgbColorRejection.COMPONENT_OUT_OF_RANGE`）。既定は `#000000`。

透明度（アルファ）は持たない。

### FR-048 — 画面の言語と UI の書体

画面の文言を**日本語と英語**で切り替えられる。時計の表示（`HH:mm:ss` / `yyyy-MM-dd`）は
この設定に影響されない。時刻の整形はロケールに依存しない（FR-006）。

- 実行環境のロケールから決めない。**保存された設定から決まる**（ARC-007 と同じ考え方）
- 文言は 1 か所（`UiText`）に集める。**1 つの定数が両方の言語を持つ**ので、
  片方だけ足してもう片方を忘れることができない
- `ResourceBundle` を使わない。既定ロケール（＝実行環境の状態）を読む経路が生まれるため
- **UI の書体も同梱する**（`InterfaceTypeface`）。言語から決まり、利用者は直接選ばない

| 言語 | 書体 | 選んだ理由 |
| --- | --- | --- |
| 日本語 | Zen Kaku Gothic New | 静的な Regular がある日本語ゴシック |
| English | Arimo | Arial とメトリック互換。ラテン文字の UI として無理がない |

🔴 **Noto Sans JP は採れなかった。** 上流には可変版しか無く、9.6 MB あって
既定インスタンスが Thin である（Java 21 は可変フォントの軸を選べない・[ADR 0006](docs/adr/0006-typefaces-are-bundled-not-discovered.md)）。

### FR-047 — ウィンドウの見た目と操作

OS のタイトルバーを持たない。時計は「操作する道具」ではなく「置いてあるもの」として見えること
（[ADR 0008](docs/adr/0008-the-window-is-the-clock.md)）。

- 通常は時刻と日付だけが見える。窓の枠・タイトル・ボタンは**出ていない**
- **ホバーしたときだけ**、右上に操作用のクロームが現れる。移動 / 設定 / 終了の 3 つ
- クロームは時刻の表示に重ならない。現れるときは 140ms、消えるときは 240ms でフェードする
- **窓のどこを掴んでもドラッグで動く。** 移動アイコンは「掴める」ことを教える役目であり、唯一の経路ではない
- 終了アイコンで閉じる。閉じたときに常駐スレッドを残さない（FR-030）
- 角丸は環境が対応していれば適用し、対応していなければ角のまま描く。
  半透明の可否を読む手段はアプリに無いので（ADR 0006）、**試して断られたら諦める**形にする
- クロームの色は時計の文字色に従う。クロームは時計の一部であって、別の世界の部品ではない

### FR-046 — 背景色

時計の背景色を設定できる。値の規則は文字色と同じ（`RgbColor`・各成分 0..255・透明度なし）。
既定は `#F5F2EB`。

- **文字色と背景色は同じ型で表す。**「各成分が 0..255」という規則は 1 つであり、
  型を 2 つに分けると同じ規則が 2 か所に存在することになる（[ADR 0007](docs/adr/0007-one-colour-type-two-roles.md)）
- **読めない組み合わせ（黒地に黒など）を禁止しない。** 選べるが、設定画面が警告する。
  設定は利用者のものであり、アプリが上書きしない
- 背景の透明化（半透明ウィンドウ）は行わない。環境依存で「無くても成立する」ことを保てないため

### FR-045 — 設定画面

設定はタブではなく、**ギアのアイコンから開くモーダル**である（FR-047 / [ADR 0008](docs/adr/0008-the-window-is-the-clock.md)）。
モーダルから FR-040 の 9 項目すべてを変更できる。

モーダルは 3 つの画面を持つ。選択肢が多いものだけを別画面へ出す。

| 画面 | 内容 |
| --- | --- |
| 一覧 | 時刻表記・秒・日付・最前面・言語・大きさ。その場で決まる |
| 書体 | 30 書体を**その書体自身で** `12:34` と描いて選ばせる。雰囲気で絞り込める |
| 色 | 厳選プリセット 24 色と HEX の直接指定。文字色と背景色で同じ画面を使う |

- 書体は同梱の 30 書体から**選ぶ**。自由入力にしない
- フォントサイズは FR-041 の範囲内でしか選べない
- 文字色と背景色のコントラストが破綻したら**警告する。選択は妨げない**（FR-046）。
  読める色への修正を 1 操作で提案する
- 変更は即座に時計の表示へ反映され、そのまま保存される。OK / キャンセルを持たない
- 保存に失敗したときは利用者に分かる形で伝える。スタックトレースを見せない（FR-042）
- モーダルの配色は**時計の背景色の明るさに追従する**。明るい時計なら明るいモーダルになる
- 既製の Swing 部品の見た目（Metal / Nimbus）を出さない。つまみ・分節・帯・札は自分で描く

### FR-042 — 設定の永続化と版

設定は `java.util.prefs.Preferences` に保存し、再起動後も残る。
保存形式は版（`SettingsSchemaVersion`）を持つ。現在の版は **5**。

| 保存されている版 | 扱い |
| --- | --- |
| 1（書体・文字色が無い） | **移行して読む。** 既存の 5 項目は保たれ、書体と文字色は既定値になる |
| 2（実行環境の書体名を持つ） | **移行して読む。** 同じ名前の同梱書体があればそれを引き継ぎ、無ければ書体だけ既定へ落とす。ほかの 6 項目は保たれる |
| 3（背景色が無い） | **移行して読む。** 既存の 7 項目は保たれ、背景色は既定値になる |
| 4（言語が無い） | **移行して読む。** 既存の 8 項目は保たれ、言語は既定値になる |
| 5 | そのまま読む |
| 6 以上（未来の版） | `UNSUPPORTED_SCHEMA`。推測して読まない |

移行の方針は [ADR 0003](docs/adr/0003-settings-schema-migration.md)。

読めなかった場合は、理由を失わずに既定値へ落とす。

| 状況 | 返す失敗 |
| --- | --- |
| 保存がまだ無い | `ABSENT` |
| 保存領域が読めない | `UNREADABLE` |
| 版が対応外 | `UNSUPPORTED_SCHEMA` |
| 値が不変条件を満たさない | `INVALID_VALUE` |

保存の失敗も戻り値の無い `void` で消さず、`SettingsSaveOutcome` で返す。

### FR-050 — 画面構成

**タブを持たない。** 窓そのものが時計であり、設定はモーダルである（FR-045 / FR-047）。

M1 で足すストップウォッチとタイマーの置き場所は、**実装の直前に決め直す**。
タブに戻すのか、クロームにもう 1 つ経路を足すのか、別の窓にするのかは、
中身ができてから判断する。空の入れ物を先に置かない（使えない UI を「ある」ように見せないため）。

| 機能 | 状態 |
| --- | --- |
| 時計 | 窓そのもの |
| 設定 | ギア → モーダル（FR-045） |
| ストップウォッチ | M1（FR-010 と同時。置き場所は未定） |
| タイマー | M1（FR-020 と同時。置き場所は未定） |

---

## 4. 完了条件

### M0（現在のマイルストーン）

- `./gradlew check` が通る
- `./gradlew run` で時計が表示される
- 時刻表示・12/24 切り替え・秒表示・日付表示が仕様どおりに動く（単体テストで検証）
- 設定が保存・復元される。壊れた保存値を型のある失敗として扱える
- CI が同じ `check` を実行して緑になる
- 規範文書と強制マトリクスが実装と一致している（CNF-005）

### M0.1（設定 GUI）

- FR-043 書体・FR-044 文字色を値型として持つ（書体はのちに同梱書体の列挙へ変わった）
- FR-045 設定画面から 7 項目すべてを変更でき、即座に反映され保存される
- FR-042 保存形式 v2 と v1 からの移行（既存の設定を失わない）

### M1

- FR-010 ストップウォッチ
- FR-020 カウントダウンタイマー
- FR-050 の残り 2 タブ（Stopwatch / Timer）
- SWG-004（レイアウト規則）と SWG-005（減算カウンタ禁止）の機械強制を `planned` から `active` へ

### 0.1.0 リリース

M1 の全項目に加えて、README の完成と、実機（WSLg）での表示確認の記録（QLT-012）。

---

## 5. 将来の候補（着手しない）

トレイ常駐、複数アラーム、コンパクト表示、透過ウィンドウ、キーボード操作、
音の選択、世界時計、タイムゾーン選択。

これらは M1 が完了するまで実装しない。
